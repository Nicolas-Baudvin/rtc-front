import Header from '../../Components/Header';
import './style.scss';

function ChatRoom() {
    return (
        <main className={'chat'}>
            <Header page={'Chat'} />
        </main>
    );
}

export default ChatRoom;
