import Header from '../../Components/Header';

function JoinChat() {
    return (
        <div className={'join'}>
            <Header page={'Rejoindre un salon'} />
        </div>
    );
}

export default JoinChat;
